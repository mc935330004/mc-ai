# PM 业务智能助手领域词汇表

本词汇表统一 PM 项目及相关业务数据查询、会话分析和报告导出中的业务语言，避免权限范围、报告主体与项目责任关系混用。

> 说明：本文件位于隔离工作树 `business-assistant`。TASK20（可配置数据契约与项目期间关联）相关的术语已按已通过测试的实现补充，外部真实数据接入状态标记为「待管理员配置」。

## Language

**业务助手 Agent**：
唯一面向用户进行对话理解和结果表达的 AI 入口。它识别项目全景、出差、打卡、报销和组合报告等业务场景，通过确定性工作流取得可信数据，不为业务模块创建子 Agent。
_Avoid_: 项目助手 Agent、多 Agent 集群、模块 Agent

**报告主体**：
一份组合报告用于组织查询条件、统计口径和章节内容的核心业务对象，可以是一个项目、一个人员或一个部门，由用户描述明确选择。
_Avoid_: 当前登录用户、固定项目主体、报告模板

**报告主体候选列表**：
人员名称或部门名称命中多个当前用户有权查看的对象时返回的分页选择列表，只展示员工编码、部门路径等允许披露且足以区分候选项的字段。用户明确选定主体前，不执行该主体的业务查询和组合报告。
_Avoid_: AI 自动选主体、接口首条记录、越权候选列表

**可查询人员范围**：
来源系统根据当前登录人的角色返回的人员集合。普通员工的范围仅包含本人，管理员或其他特殊权限用户可以取得来源系统允许的人员列表；业务助手不能在该集合之外使用工号直接查询人员数据。
_Avoid_: 全体员工、项目成员列表、本地角色推导范围

**人员业务查询**：
在可查询人员范围内，以一个明确员工工号为主体执行的出差、打卡或报销等只读查询。当前接口一次只查询一个员工，不表示支持部门或多人明细批量查询。
_Avoid_: 多人员批量查询、姓名直接查询、越权工号查询

**多人报告查询批次**：
部门或多人组合报告将授权人员集合拆分后形成的异步执行单元，按工号受控调用人员业务查询。系统对并发、速率、超时和单任务人数设置可配置上限，超出范围时要求缩小条件或拆分任务，不能对全部人员无限扇出。
_Avoid_: 无限制并发、同步多人查询、伪批量接口

**多人报告摘要**：
部门或多人组合报告默认呈现的汇总层，包含总体指标、允许披露的异常人员以及数据完整性，不自动展开所有人员业务明细。人员级明细只有在模板配置或用户明确要求且权限、字段策略允许时才进入导出。
_Avoid_: 默认全员明细、聊天全量展开、无权限明细导出

**业务查询快照**：
一次只读业务查询经过标准事实映射、字段过滤和脱敏后形成的可信结果记录，包含报告主体、查询条件、查询时间、数据来源、到期时间以及成功、失败或数据缺失状态。全局有效期最长24小时，报告数据集可以配置更短期限；原始业务响应和禁止使用的字段不得进入快照。项目全景快照是面向单个项目的一种业务查询快照。
_Avoid_: 原始接口响应、聊天文本、AI 摘要、报告文件

**组合报告**：
围绕一个明确报告主体，引用一个或多个业务查询快照形成的跨业务域报告。它可以组合项目全景、出差、打卡和报销等章节，业务事实和统计由后端确定性产生，AI只生成摘要和解释。
_Avoid_: 项目报告、聊天记录导出、AI 自由生成报告数据

**组合报告补查**：
组合报告计划缺少所需业务查询快照时，对缺失章节执行对应已注册只读工作流并生成快照的过程。主体和条件一致、未过期且权限复核通过的已有快照优先复用；用户明确要求最新、刷新或重新查询时不复用相关旧快照。
_Avoid_: 先逐项人工查询、每次全量重查、跨主体复用快照

**快照确定性裁剪**：
从主体相同、权限复核通过且查询范围覆盖目标条件的业务查询快照中，依据已保存的结构化字段生成更窄结果的复用方式。只有报告数据集声明字段粒度足以可靠筛选时才允许裁剪；聚合粒度不足或条件无法证明覆盖时必须重新查询，不能交给 AI 判断。
_Avoid_: AI 筛选原始数据、模糊条件复用、跨主体裁剪

**组合报告模板**：
管理员按项目、人员或部门等报告主体类型维护的当前生效默认章节和展示结构。用户可以在默认范围上明确增加、删除或筛选章节；AI识别业务章节意图，但不直接选择或拼装底层工作流。
_Avoid_: 全主体统一模板、每次强制追问、AI 自由选择工作流

**报告数据集**：
组合报告中的一个已注册业务章节定义，声明业务语义、适用主体类型、查询参数以及对应的已发布只读工作流。报告计划选择报告数据集，后端再确定性解析并执行工作流。
_Avoid_: 裸工作流、任意能力、AI 工具调用

**报告字段策略**：
报告数据集对标准事实字段声明的使用边界，分别规定字段是否可参与后端统计、进入对话展示、写入导出文件以及是否需要脱敏。业务接口鉴权通过不等于其全部返回字段都可披露，AI只能接收策略允许展示的事实和已完成的确定性统计结果。
_Avoid_: 接口全字段透传、模型自行脱敏、查询权限等同披露权限

**原始业务响应**：
来源业务接口在一次工作流执行中返回、尚未经过字段策略处理的数据，只允许在执行内存中用于校验、映射、过滤和确定性统计。处理完成后立即丢弃，不得写入快照、结果制品、日志、模型上下文或报告文件。
_Avoid_: 可信快照、调试日志、原始数据归档

**直接项目关联**：
业务记录自身携带经过校验的项目编码或项目 ID，能够明确归属于一个项目的关联关系。只有直接项目关联的数据才可以按既定业务口径计入项目工时、项目费用等归属统计。
_Avoid_: 人员期间推断、名称模糊匹配

**项目人员期间关联**：
业务记录未携带项目标识，但记录人员在项目成员关系有效期间内属于该项目时形成的间接上下文关系。报告必须明确标注其为项目人员期间数据，不得据此把打卡、出差或报销直接归属为项目工时、项目行为或项目成本。
_Avoid_: 直接项目关联、AI 推断归属、项目费用认定

**业务域查询权限**：
当前用户读取一个业务域数据的权限，由该业务域的来源系统根据可信登录上下文中的当前用户角色以及目标主体、资源和数据范围判定。来源系统可以只返回允许或拒绝而不返回业务明细；项目、出差、打卡和报销权限彼此不继承，业务助手只透传登录上下文，不接受对话声明的角色，也不在本地复制、扩张或推导权限。
_Avoid_: 对话角色声明、统一 Agent 数据权限、项目权限跨域继承、本地权限缓存

**业务快照权限复核**：
读取业务查询快照进行追问、组合、报告生成或文件下载前，调用快照对应来源系统的独立权限校验能力，重新确认当前用户仍有权读取相关主体、资源和数据范围，不为权限复核重新获取全部业务明细。任一来源系统校验拒绝或不可用时，对应快照失败关闭，不能以快照创建时曾经有权为理由继续访问。
_Avoid_: 创建时授权、跨业务域统一授权、Agent 缓存权限

**受限组合报告**：
请求的一个或多个业务章节因当前用户权限不足而未被查询和纳入的组合报告。其他已授权章节可以继续生成，但报告必须标明受限章节，且不能泄露其数据是否存在、记录数量或任何业务内容。
_Avoid_: 空数据章节、完整组合报告、越权补全报告

**项目编码**：
PM 系统中唯一标识一个具体项目的稳定业务编码，例如 `XXXT2674040`，用于项目定位、权限校验和跨模块查询。
_Avoid_: 项目类型、项目名称

**项目类型**：
PM 系统返回的稳定项目分类，用于匹配项目全景方案。项目类型描述一类项目，不用于唯一定位具体项目。
_Avoid_: 项目编码、项目编号

**项目年度**：
PM 项目基本信息中的年度属性，用于从当前用户的可访问项目中筛选项目集合。项目年度不是合同、概算、现金流、产值等模块的查询期间，项目选定后不自动下沉为模块参数。
_Avoid_: 模块统计期间、数据发生年度

**报告查询期间**：
出差、打卡、报销等时序报告数据集使用的业务发生时间范围。用户未指定时默认为当前自然年；只有用户明确要求“项目期间”时，才使用已选项目的起止日期，且该期间不传给没有时间参数的项目全景模块。
_Avoid_: 项目年度、隐式项目全周期、所有模块统一时间参数

**项目期间**：
用户明确表达「按项目起止日期查询」时启用的查询语义，由 `projectPeriodRequested` 标志表达。项目起止日期不来自模型生成，而是由 `ProjectPeriodContextService` 独立复核人员与项目权限后，从 `PROJECT_BASE` 数据集的安全事实中确定性取得；项目期间结果覆盖查询计划的日期范围。数据源未配置或日期不完整时安全收尾，不执行后续业务查询。
_Avoid_: 模型生成日期、项目年度当期间、无条件按项目全周期查询

**项目选择范围**：
当前用户的可访问项目经过项目年度等项目基础信息条件筛选后形成的候选集合，供项目组合概览和项目定位使用。用户没有指定年度时，默认范围为当前年度的可访问项目。
_Avoid_: 模块查询条件、项目全景结果

**项目定位条件**：
用户用于从项目选择范围中查找项目的项目名称、项目编码或项目经理条件。所有定位条件都必须叠加当前用户的项目查看权限和项目年度，不能绕过 PM 权限直接定位项目。
_Avoid_: 模块筛选条件、无权限项目搜索

**项目候选列表**：
项目名称或项目经理等非唯一条件命中多个可访问项目时返回的分页选择列表。用户明确选定项目之前，不对候选项目执行项目模块工作流。
_Avoid_: 自动批量项目全景、AI 自动选项目

**可访问项目**：
当前登录用户在本次查询时，经 PM 系统数据权限判定后有权查看的项目。“我的项目”在用户会话中统一解释为全部可访问项目，不表示用户本人负责、参与或所属部门管辖的项目。
_Avoid_: 我的项目、管辖项目、负责项目

**项目查看权限**：
PM 系统授予当前用户读取一个项目及其合同、概算、现金流、产值等关联模块信息的权限。项目内各模块不再设置独立的查询权限；该术语不包含新增、修改、审批或删除权限。
_Avoid_: 模块查询权限、Agent 项目权限

**项目组合概览**：
基于当前用户全部可访问项目形成的分页基本信息视图，只使用项目列表接口已经返回的数据，不逐项目查询合同、概算、现金流、产值等模块。
_Avoid_: 我的项目全量分析、批量项目全景

**项目全景**：
针对用户已经明确选定的单个项目，聚合其合同、概算、现金流、产值等模块信息形成的完整项目视图。第一版一次只允许执行一个项目全景。
_Avoid_: 项目组合概览、全部项目分析、多项目全景

**项目全景方案**：
管理员通过管理页面维护的当前生效项目全景默认查询模块集合；项目类型没有专属配置时使用全局默认方案。方案保存后立即用于新查询，不保留历史配置版本，也不由 AI 临时猜测。
_Avoid_: 写死模块、AI 动态默认模块

**项目全景快照**：
一次项目全景查询形成的可信结果记录，至少包含实际执行模块、查询时间以及各模块成功或失败状态，供同一会话后续分析和报告导出使用。快照保留 24 小时，过期后必须重新查询 PM；它记录当次数据依据，不是项目全景方案的历史版本。
_Avoid_: 配置历史、聊天文本历史

**快照追问**：
同一会话围绕已选项目继续进行筛选、统计、解释或报告导出时，默认读取上一轮项目全景快照。只有用户明确要求刷新、重新查询或获取最新数据时，才重新调用 PM 并生成新快照。
_Avoid_: 每轮重新查询、跨会话自动继承

**项目全景总览回答**：
项目全景首次查询返回的精简回答，只展示项目基本事实、关键指标、异常和数据缺失。完整安全明细保存在项目全景快照中，后续按用户追问或报告导出需求读取。
_Avoid_: 全量明细回答、完整报告

**渐进式项目全景回答**：
项目基础信息先展示，各项目模块工作流完成后持续更新模块状态；全部模块进入成功、失败或超时终态后，再保存快照、执行项目问题判定并输出统一总结。
_Avoid_: 全部完成后一次性返回、未完成数据总结

**项目报告导出**：
根据用户明确指定的格式，从权限复核通过且未过期的项目全景快照生成 XLSX、DOCX 或 PDF 文件。导出使用快照中的可信事实和安全明细，不重新让 AI 生成业务数据。
_Avoid_: 聊天文本另存、AI 编造报表数据

**项目报告模板**：
报告主体为项目时使用的组合报告模板特化，可继续按项目类型匹配并在没有专属配置时使用项目全局默认模板。它与人员、部门模板共用报告数据集和格式渲染机制，不构成另一套报告系统。
_Avoid_: 独立项目报表系统、AI 动态报表结构、全系统固定模板

**不完整项目报告**：
基于存在模块失败或数据缺失的项目全景快照生成的报告。文件首页、对应章节和元数据必须显著标注数据不完整、失败模块和查询时间；缺失数据不能按零展示，也不能据此生成完整性结论。
_Avoid_: 完整报告、空值补零报告

**项目报告导出任务**：
根据项目报告模板和项目全景快照异步生成一个指定格式文件的后台任务。任务绑定当前用户、会话、项目和快照，并向聊天返回生成状态以及成功后的受控下载入口；生成文件保留24小时，每次下载重新校验当前用户和项目权限。
_Avoid_: 同步长请求、匿名下载链接

**项目问题判定**：
后端使用已配置的确定性规则，对项目全景快照中的可信字段产生问题命中、未命中或无法判断的结论。AI只解释判定依据，不生成、删除或修改正式问题结论；没有规则或字段不足不等于没有问题。
_Avoid_: AI 风险判断、主观问题分析

**跨模块问题规则**：
在全部项目模块工作流完成后，比较两个或多个模块可信字段的问题判定规则。任一依赖模块失败或字段缺失时，规则结果为无法判断，不把缺失值按零处理。
_Avoid_: 单模块规则、缺失值补零规则

**标准事实字段**：
项目模块工作流对外声明的稳定、全局唯一语义字段，包含字段编码、数据类型及必要的业务口径。项目问题规则、总览回答和报告引用标准事实字段，不直接引用工作流原始 JSON 路径。
_Avoid_: 原始 JSON 路径、接口字段名

**项目模块工作流**：
已发布、启用且只读的工作流，由项目全景方案通过工作流编码选择。它在内部组合已注册 READ 能力，并封装分页、列表转详情和模块结果汇总；项目全景不直接编排工作流内部能力。
_Avoid_: 裸 READ 能力、WRITE 工作流、AI 选择的工作流、子 Agent

**项目标识参数映射**：
管理员在项目全景方案中，将规范化项目编码映射到所选项目模块工作流输入 Schema 的一个具体字段。映射在方案保存时校验，运行时按映射确定性传参，不由字段名规则或 AI 猜测。
_Avoid_: 参数名白名单、AI 参数映射

**双主体会话上下文**：
人员与项目作为两个独立主体分别解析和保存选择令牌（`personSelectionToken`、`projectSelectionToken`），互不混淆。下一轮追问继承两种令牌；显式新项目编码只清除继承的项目令牌，不清除已确认人员令牌。通用 `selectionToken` 仅作最低优先级兼容。
_Avoid_: 单一 selectionToken、项目编码当作人员定位条件、新项目清除人员令牌

**项目关联分类**：
对业务记录做纯 Java 规则分类，输出 `DIRECT`、`PROJECT_PERSON_PERIOD`、`UNKNOWN`、`UNRELATED` 四类。关联服务只返回分类，不计算金额；金额统计由确定性聚合服务在分类之后完成。
_Avoid_: 关联服务内计算金额、AI 推断分类、缺失身份归为无关

**报告文件处理器**：
组合报告后台任务产出 XLSX、DOCX、PDF 文件的确定性组件。它只消费安全快照的 `export` 通道和当前字段策略，复用已有渲染器与 `SafeArtifactStorageService`，不读取来源系统原始响应；快照结构不合法时失败关闭，交由任务重试机制接管。
_Avoid_: 直接读取接口响应、硬编码字体路径、AI 生成报告文件
