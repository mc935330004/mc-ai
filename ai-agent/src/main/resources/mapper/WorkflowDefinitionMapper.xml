<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.ai.agent.workflow.mapper.WorkflowDefinitionMapper">

    <select id="pageWorkflows"
            resultType="org.example.ai.agent.workflow.entity.WorkflowDefinition">
        SELECT * FROM ai_workflow_definition
        <where>
            <if test="keyword != null and keyword != ''">
                AND (
                workflow_code LIKE CONCAT('%', #{keyword}, '%')
                OR workflow_name LIKE CONCAT('%', #{keyword}, '%')
                OR description LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
            <if test="publishStatus != null and publishStatus != ''">
                AND publish_status = #{publishStatus}
            </if>

            <if test="enabled != null">
                AND enabled = #{enabled}
            </if>
        </where>
        ORDER BY updated_at DESC
    </select>

    <select id="selectByIdForUpdate"
            resultType="org.example.ai.agent.workflow.entity.WorkflowDefinition">
        SELECT * FROM ai_workflow_definition
        WHERE id = #{id}
        FOR UPDATE
    </select>
</mapper>