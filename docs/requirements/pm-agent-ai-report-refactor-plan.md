# PM Agent - AI Report 重构与访问控制闭环记录

## 1. 文档说明

原需求文档已在提交 `c9a3a6e` 中删除。本文件以精简方式恢复当前有效的实施状态，不恢复已经失效的逐轮代码示例，避免历史方案继续误导开发。

## 2. 已闭环能力

### 2.1 AI Report 主链

- 后端业务结果统一转换为 `ReportSchema`，前端固定组件负责渲染。
- `DATA_QUERY` 直接展示业务报告，`ANALYSIS_REPORT` 在基础报告后追加 AI 分析。
- 专用 Java `ReportTemplate` 优先，后台 `ReportDefinition` 作为普通模块的通用配置能力。
- 报告支持固定区块、普通列表、树形数据、文件字段和分析状态。
- 报告后的二次问答复用上一次安全结果快照，不依赖前端回传完整业务数据。

### 2.2 模型配置

- 支持系统模型配置、人员模型授权、聊天选模和失败切换。
- 支持模型连通性测试、配置缓存失效、乐观锁和配置审计。
- 旧版 `/api/aiAgent/**` 模型直连接口默认关闭，只允许通过环境变量临时回滚开启。

### 2.3 能力与工作流人员访问控制

- 能力和工作流统一支持 `PUBLIC` 与 `RESTRICTED` 两种运行范围。
- `PUBLIC` 对所有已登录人员开放；`RESTRICTED` 仅允许名单中的 PM 用户编码运行。
- 候选召回阶段先过滤无权资源，防止无权资源进入模型选择上下文。
- 能力、工作流、失败重试、WRITE 二次确认、动态选项和文件上传在真正执行前再次校验权限。
- 权限配置绑定资源定义，不绑定发布版本；发布新版本后继续沿用当前访问策略。
- 管理端编辑、发布和能力测试由管理员权限保护，不要求管理员加入业务运行名单。
- 保存权限时锁定资源定义并在同一事务内替换授权名单，避免并发覆盖产生半完成状态。

### 2.4 知识库租户与部门隔离

- 知识文档写入服务端认证得到的 `tenantId`，不接受前端伪造租户和部门标识。
- 支持租户内公开 `PUBLIC` 和本部门可见 `DEPARTMENT`。
- RAG 只检索当前租户、当前人员可见、已经发布且有当前版本的文档。
- 文档列表、概览、状态变更、版本发布、重新向量化、切片列表和切片启停均校验租户。
- 问答日志、统计、详情和引用记录均按当前租户隔离，并记录实际提问人。
- Agent 异步聊天在请求线程捕获可信知识库身份，避免异步线程读取已经失效的 `HttpServletRequest`。
- 仅携带旧 `Authorization` 的普通业务聊天保持兼容；真正进入知识库查询时必须存在有效 Agent 会话。

### 2.5 接口安全与契约整理

- Agent、知识库和旧版 AI 接口统一进入登录与管理员边界。
- 普通用户仅访问聊天、待确认操作、动态表单和知识库问答接口，其余管理接口要求 `ai_agent_admin`。
- Cookie 写请求校验浏览器 `Origin`，降低跨站请求风险。
- 知识分类删除改为 `DELETE`，不再用 `GET` 执行状态变更。
- 已清理前端中没有后端实现且没有页面调用的知识分类详情旧接口、向量任务详情和重试旧接口。

## 3. 数据库变更

本轮数据库内容统一追加在：

`ai-agent/src/main/resources/db/migration/V11__create_action_audit_log.sql`

包含：

- 能力和工作流 `access_scope` 字段。
- `ai_agent_resource_user_grant` 人员授权表。
- 知识文档 `tenant_id`、`access_scope`、`owner_dept_id` 字段及索引。
- 知识问答日志 `tenant_id`、`user_id` 字段及索引。

当前 Flyway 已关闭，发布前必须手工执行对应 SQL。历史知识文档的 `tenant_id` 无法可靠自动推断，必须按真实租户归属补齐；未补齐的数据不会参与新隔离规则下的列表和检索。历史问答日志保持空租户，不自动填充虚假归属。

## 4. 当前限制与风险

- 人员权限当前以 PM 登录账号区分大小写保存；项目未发现可复用的 PM 人员搜索接口，因此配置页只校验格式和数量，不能在保存前验证账号是否真实存在。
- 能力和工作流授权当前是全局资源策略，不按租户分别配置。如果未来同一资源需要不同租户维护不同名单，需要为授权表和管理接口增加租户维度。
- 当 `agent.sso.enabled=false` 时，统一身份和管理员拦截按现有兼容策略不生效；生产环境必须保持 SSO 开启。
- 本轮按约定只做静态检查，没有运行 Maven 编译、测试、Flyway、前端构建、接口请求或浏览器验收，不能把这些运行结果表述为已经通过。

## 5. 发布前人工核对

1. 手工执行 V11 中本轮新增的访问控制和知识库隔离 SQL。
2. 按真实租户归属补齐历史 `knowledge_document.tenant_id`，不要对多租户数据做统一错误回填。
3. 确认生产环境 `agent.sso.enabled=true`，并正确配置允许的浏览器 Origin。
4. 用未配置名单的普通人员验证受限能力和工作流不会被召回，也不能通过直接执行、重试或确认操作绕过。
5. 用已授权人员验证能力、工作流、动态选项和文件上传可以正常执行。
6. 分别使用两个租户和两个部门验证知识文档、切片、问答日志和 RAG 命中结果互不可见。
7. 验证普通业务聊天、知识库问答、报告生成和历史恢复没有受到访问控制改造影响。

## 6. 当前阶段状态

- Phase 6：代码实现与静态收口已完成。
- AI Report 与访问控制最终开发闭环：已完成。
- 运行验收闭环：等待按第 5 节执行人工验证后确认。
- 工作流与报告傻瓜式快速配置已按用户要求完成代码回退；保留原工作流编排、手工报告配置、调试、保存、发布及正式报告链路。
- 现有手工报告配置“字段路径自动补全”设计已确认：路径继续展示但由区块类型、当前 GraphSpec 和字段字典确定性生成，不允许手工维护。
- 手工报告配置字段路径自动补全已完成前端实现：支持主流程、单层 `FOREACH` 和现有双层列表详情结构，自动生成基础信息/核心指标的对象路径和绝对字段路径，以及普通列表/树形列表的数据行路径和相对字段路径。
- 路径控件保留只读展示；新增、删除、更换字段或切换区块类型时自动重算，历史区块未修改时继续回显原路径，不会因打开弹窗自动改写配置。
- 同一区块只允许选择能进入最终结果的同一能力、同一业务对象或同一列表数据行字段；无法唯一推导的自定义输出和不同对象字段明确阻止，不猜测路径。
- 路径生成已与后台 `CapabilityFieldPathProjector` 对齐：保留 `workflowData` 中的数组层级，普通对象层级按稳定机器字段名扁平化；详情执行结果按 `items[].data` 定位，循环来源记录按 `items[].item` 定位。
- 已修复列表详情流程只显示详情能力字段的问题：路径解析同时识别 `FOREACH.itemsExpression` 对应的列表能力和循环体详情能力，列表字段从最终结果的 `items[].item` 读取，详情字段继续从 `items[].data` 读取。
- 上述手工报告配置字段路径自动补全阶段仅修改前端，没有修改后端、数据库、Flyway、Redis 或正式报告协议；按约定未运行测试、构建和浏览器验收。
- 字段字典列表优化已完成代码实现：列表不再展示字段路径，但新增、编辑和详情仍保留字段路径；字段用途、回答规则、可搜索、可聚合支持列表开关即时保存，接口失败会恢复原值。
- 字段字典空值规则已统一：数字类型固定为 `0`，其他类型为空；新增、编辑、自动生成和批量确认均由后台强制执行，V11 末尾已提供历史数据收口 SQL。
- 字段字典筛选条件、当前页和每页条数已写入路由参数，进入新增、编辑或详情后返回列表可恢复原查询状态。
- 本次未修改表结构、工作流和报告协议；仅完成静态语法、调用链和差异检查，未运行 Maven 编译、前端测试或构建，等待用户执行页面和运行验收。
- 字段字典列表优化当前为“代码完成、待用户验收”，尚未最终闭环；用户确认页面和运行结果后再记录最终闭环总结。
- 模型调用监控“最近失败”已改为数据库分页查询：默认每页 10 条，支持 10、20、50 条；切换统计范围或每页条数时回到第一页，模型汇总和按模型统计逻辑保持不变。
- 最近失败分页未新增数据库表或索引，继续只展示失败分类对应的安全摘要；当前为“代码完成、待用户验收”，本阶段未运行 Maven 编译、前端测试或构建。
- 通用报告已增加 `auditStatus` 审批专属位置：报告表头和运行状态保持不变，审批状态显示在首个基础信息区块右上角，并按 0审批中、1审批成功、2驳回、3撤回、4暂存映射颜色。
- `auditStatus` 已从普通区块字段中排除，避免继续显示“审核状态 1”原始数字；字段缺失、值无效、没有基础信息区块或多项目状态不一致时不展示。该阶段仅修改前端，等待用户页面验收。
- 报告核心指标“多字段计算公式”设计已确认：只允许结构化字段选择、SUM/AVG/COUNT/MAX/MIN 和加减乘除，不开放脚本、SQL、SpEL、括号或手工常量。
- 多字段计算第一阶段已完成并通过静态检查：结构化计算模型、聚合/运算枚举、旧配置空集合兼容和 `BigDecimal` 计算器均已落位；按约定未运行测试或编译。
- 多字段计算第二阶段已完成并通过静态检查：计算指标发布校验、字段字典收集、字段路径一致性检查和数值类型限制均已接入；按约定未运行测试或编译。
- 多字段计算第三阶段已完成并通过静态检查：`ConfigurableReportSectionBuilder` 已追加计算结果，单个指标异常只返回空值并记录不包含业务值的警告日志，旧报告和其他区块构建逻辑保持不变。
- 多字段计算前端配置器已完成代码实现和静态检查：核心指标支持直接字段与计算指标共存，可配置自定义名称、NUMBER/AMOUNT/PERCENT、SUM/AVG/COUNT/MAX/MIN、加减乘除和多个计算项；字段绝对路径继续复用现有工作流路径解析规则自动生成。
- 当前阶段按约定未运行后台编译、前端测试或构建；等待用户在页面保存并运行一条计算指标后进行人工验收，验收通过后输出最终闭环总结。

## 7. 全项目生产化阶段进度

详细记录见：

`docs/requirements/production-safety-quality-closure.md`

当前状态：

- 第一阶段“生产安全收口”：代码与配置约束已完成，真实账号、跨租户和HTTPS部署验收待执行；明文密钥按用户要求由用户后续处理。
- 第二阶段“质量与稳定性”：有界SSE线程池、超时取消、Redis原子限流、外部服务熔断、健康检查、数据保留、清理索引、核心单元测试、浏览器端报告回归用例和CI配置已完成，测试、压测与故障演练待执行。
- 第三阶段“产品闭环”：尚未开始本轮实施。
- 第四阶段“增强能力”：尚未开始本轮实施。
